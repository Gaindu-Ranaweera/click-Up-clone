import './bootstrap';
import * as bootstrap from 'bootstrap';

// Alpine is default in Breeze, we can arguably keep it or remove it.
// The user asked for Bootstrap, but Alpine is useful for small interactions.
// I'll keep the import if it was there, but for now I'm replacing the file to ensure bootstrap is there.
// If I need Alpine I'll add it back.
